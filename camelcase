class Solution {

    // Complete the camelcase function below.
    static int camelcase(string s)
     {
      int count = 1;
    for (int i = 0; i < s.Length; ++i)
    {
        if (s[i] <97)
        {
            count++;
        }
    }
    return count;
     }
